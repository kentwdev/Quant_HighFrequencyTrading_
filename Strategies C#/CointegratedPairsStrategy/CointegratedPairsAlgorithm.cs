using System;
using System.Linq;
using Strategies.VxvPricePredictionStrategy;

namespace Strategies.CointegratedPairsStrategy
{
    public class CointegratedPairsAlgorithm
    {
        /*static void Main(string[] args)
        {
            Random randNum = new Random();
            var a = Enumerable
                .Repeat(0, 100)
                .Select(i => randNum.NextDouble())
                .ToArray();

            Console.WriteLine(new AugmentedDickeyFuller(a).NeedsDiff);
            Console.ReadKey();
        }*/
    }
}